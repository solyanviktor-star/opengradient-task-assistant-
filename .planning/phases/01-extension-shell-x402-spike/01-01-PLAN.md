---
phase: 01-extension-shell-x402-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wxt.config.ts
  - package.json
  - tsconfig.json
  - entrypoints/background.ts
  - entrypoints/popup/index.html
  - entrypoints/popup/main.tsx
  - entrypoints/popup/App.tsx
  - entrypoints/popup/style.css
  - public/icon-16.png
  - public/icon-48.png
  - public/icon-128.png
autonomous: true

must_haves:
  truths:
    - "Extension installs in Chrome via chrome://extensions in developer mode without errors"
    - "Extension icon appears in the Chrome toolbar after installation"
    - "Clicking the extension icon opens a popup window with placeholder content"
    - "Service worker initializes without errors and logs lifecycle events to the console"
  artifacts:
    - path: "wxt.config.ts"
      provides: "MV3 manifest configuration with name, permissions, host_permissions"
      contains: "defineConfig"
    - path: "entrypoints/background.ts"
      provides: "Service worker with lifecycle event handlers"
      contains: "defineBackground"
    - path: "entrypoints/popup/index.html"
      provides: "Popup HTML shell with React mount point"
      contains: "div id=\"root\""
    - path: "entrypoints/popup/App.tsx"
      provides: "Root React component for popup UI"
      contains: "export default function App"
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "wxt"
  key_links:
    - from: "entrypoints/popup/main.tsx"
      to: "entrypoints/popup/App.tsx"
      via: "React createRoot render"
      pattern: "createRoot.*render.*App"
    - from: "entrypoints/popup/index.html"
      to: "entrypoints/popup/main.tsx"
      via: "script type=module src"
      pattern: "script.*src.*main\\.tsx"
---

<objective>
Scaffold a working Chrome MV3 extension using WXT with React that installs, shows an icon in the toolbar, opens a popup, and runs a service worker with lifecycle event handling.

Purpose: Deliver the foundation that all subsequent plans build on. Without a working extension shell, the x402 spike (Plan 02) has nothing to run in.
Output: An installable Chrome extension with popup UI, service worker, and correct MV3 manifest.
</objective>

<execution_context>
@C:/Users/Виктор/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Виктор/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-extension-shell-x402-spike/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold WXT project with React and configure MV3 manifest</name>
  <files>
    wxt.config.ts
    package.json
    tsconfig.json
    entrypoints/popup/index.html
    entrypoints/popup/main.tsx
    entrypoints/popup/style.css
  </files>
  <action>
    1. Run `pnpm dlx wxt@latest init opengradient-task-assistant --template react` in the project root directory (C:/Users/Виктор/Desktop/Билд для опен градииента). If the project root already has files, scaffold into a subdirectory and move contents up, or run inside the existing directory as appropriate.

    2. After scaffolding, install dependencies: `pnpm install`

    3. Install x402 and Ethereum libraries (needed by Plan 02 but declared now to avoid file ownership conflicts on package.json):
       `pnpm add @x402/fetch @x402/evm viem`

    4. Install Tailwind CSS for styling:
       `pnpm add -D tailwindcss @types/chrome`

    5. Configure wxt.config.ts with the following manifest settings:
       - name: 'OpenGradient Task Assistant'
       - description: 'AI-powered task extraction with TEE-verified privacy'
       - version: '0.1.0'
       - permissions: ['storage', 'activeTab']
       - host_permissions: ['https://llmogevm.opengradient.ai/*', 'https://rpc.opengradient.ai/*']
       - modules: ['@wxt-dev/module-react']

    6. Update entrypoints/popup/style.css to import Tailwind:
       ```css
       @import "tailwindcss";
       ```

    7. Verify the popup HTML has a div#root and loads main.tsx as a module script.

    IMPORTANT: Do NOT hardcode any private keys or secrets in any file. All x402 packages are installed now but used in Plan 02.
  </action>
  <verify>
    Run `pnpm wxt build` and confirm it succeeds without errors. Check that `.output/chrome-mv3/manifest.json` exists and contains:
    - "manifest_version": 3
    - "name": "OpenGradient Task Assistant"
    - "permissions": ["storage", "activeTab"]
    - "host_permissions" includes "https://llmogevm.opengradient.ai/*"
    - "action" with "default_popup"
  </verify>
  <done>
    WXT project scaffolded, all dependencies installed, manifest builds correctly with MV3 configuration, popup entrypoint discovered by WXT.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create service worker, popup UI shell, and extension icons</name>
  <files>
    entrypoints/background.ts
    entrypoints/popup/App.tsx
    public/icon-16.png
    public/icon-48.png
    public/icon-128.png
  </files>
  <action>
    1. Create entrypoints/background.ts with defineBackground():
       - Log 'Service worker initialized' with browser.runtime.id on startup
       - Add browser.runtime.onInstalled listener that logs install/update reason
       - Add browser.runtime.onMessage listener that handles a 'PING' message type by responding with { status: 'alive', timestamp: Date.now() }
       - ALL runtime code MUST be inside the defineBackground() callback (WXT imports the file at build time in Node.js)

    2. Update entrypoints/popup/App.tsx to show:
       - Title: "OpenGradient Task Assistant"
       - A status indicator showing "Ready" by default
       - A "Ping Service Worker" button that sends { type: 'PING' } via browser.runtime.sendMessage and displays the response
       - Use basic inline styles or Tailwind classes. Width: 380px, padding: 16px.
       - This is placeholder UI -- Plan 02 will add the x402 test button and key input

    3. Generate simple extension icons (16px, 48px, 128px PNG files):
       - Create minimal placeholder icons using a canvas-based approach or use simple colored squares with "OG" text
       - Place in public/icon-16.png, public/icon-48.png, public/icon-128.png
       - If generating icons programmatically is not feasible, create them as simple SVG-to-PNG conversions or use a solid-color placeholder

    4. Verify wxt.config.ts auto-discovers the background entrypoint and icons.
  </action>
  <verify>
    1. Run `pnpm wxt build` -- must succeed without errors
    2. Verify .output/chrome-mv3/manifest.json contains "background" with "service_worker"
    3. Verify .output/chrome-mv3/manifest.json contains "action" with "default_popup"
    4. Verify .output/chrome-mv3/manifest.json contains "icons" entries
    5. Open Chrome, navigate to chrome://extensions, enable Developer Mode, click "Load unpacked", select .output/chrome-mv3/ directory
    6. Confirm: Extension appears in the list without errors, icon shows in toolbar, clicking icon opens popup with "OpenGradient Task Assistant" title, clicking "Ping Service Worker" shows a response with status "alive"
  </verify>
  <done>
    Extension installs in Chrome without errors, icon appears in toolbar, popup opens with placeholder content and working ping button, service worker initializes and responds to messages.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pnpm wxt build` succeeds
2. Load unpacked extension from .output/chrome-mv3/ in Chrome
3. Extension icon visible in toolbar
4. Clicking icon opens popup with title and ping button
5. Service worker is listed as active in chrome://extensions detail view
6. Chrome DevTools console for the service worker shows "Service worker initialized"
7. Clicking "Ping Service Worker" in popup shows alive response
</verification>

<success_criteria>
- EXTN-01: Extension has valid Manifest V3 configuration (manifest_version: 3 in output)
- EXTN-02: Service worker handles extension lifecycle events (onInstalled listener active)
- EXTN-03: Extension icon appears in Chrome toolbar (icons registered in manifest)
- EXTN-04: Popup UI opens when extension icon is clicked (action.default_popup configured)
</success_criteria>

<output>
After completion, create `.planning/phases/01-extension-shell-x402-spike/01-01-SUMMARY.md`
</output>
