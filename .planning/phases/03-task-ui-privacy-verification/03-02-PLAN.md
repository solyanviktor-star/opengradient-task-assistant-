---
phase: 03-task-ui-privacy-verification
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - entrypoints/popup/components/VerifyBadge.tsx
  - entrypoints/popup/components/TaskCard.tsx
autonomous: false

must_haves:
  truths:
    - "Each task shows a TEE verification badge indicating attestation status"
    - "Tasks with txHash show a green Verified badge that links to the block explorer"
    - "Tasks without txHash show a gray TEE badge (no link)"
    - "Clicking the Verified badge opens the OpenGradient explorer transaction page in a new tab"
  artifacts:
    - path: "entrypoints/popup/components/VerifyBadge.tsx"
      provides: "TEE verification badge with 2-tier display (Verified with link, TEE without)"
      min_lines: 25
    - path: "entrypoints/popup/components/TaskCard.tsx"
      provides: "TaskCard updated to include VerifyBadge between content and delete button"
      contains: "VerifyBadge"
  key_links:
    - from: "entrypoints/popup/components/VerifyBadge.tsx"
      to: "https://explorer.opengradient.ai/tx/"
      via: "anchor href with txHash"
      pattern: "explorer\\.opengradient\\.ai/tx/"
    - from: "entrypoints/popup/components/TaskCard.tsx"
      to: "entrypoints/popup/components/VerifyBadge.tsx"
      via: "import and render with task.txHash prop"
      pattern: "VerifyBadge.*txHash"
---

<objective>
Add TEE privacy verification badges to each task card and provide a human-verify checkpoint for the complete Phase 3 UI.

Purpose: This is the privacy verification layer -- the key differentiator of OpenGradient. Users need visual confirmation that their task extraction was processed in a TEE and that on-chain proof exists. This plan adds the VerifyBadge component and wires it into TaskCard.

Output: VerifyBadge component with 2-tier display, updated TaskCard integrating the badge, and visual checkpoint for all of Phase 3.
</objective>

<execution_context>
@C:/Users/Виктор/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Виктор/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-task-ui-privacy-verification/03-RESEARCH.md
@.planning/phases/03-task-ui-privacy-verification/03-01-SUMMARY.md

@entrypoints/popup/components/TaskCard.tsx
@lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VerifyBadge component and integrate into TaskCard</name>
  <files>entrypoints/popup/components/VerifyBadge.tsx, entrypoints/popup/components/TaskCard.tsx</files>
  <action>
**VerifyBadge.tsx:**
Create a new component that displays TEE verification status.

Props: `txHash: string | null`.

Two states:
1. **txHash is present (green "Verified" badge):** Render as an `<a>` tag linking to `https://explorer.opengradient.ai/tx/${txHash}`, opens in new tab (`target="_blank" rel="noopener noreferrer"`). Style: inline-flex, items-center, gap-0.5, px-1.5, py-0.5, rounded, text-[10px], font-medium, bg-green-100, text-green-700, hover:bg-green-200, no-underline. Include a small shield/checkmark SVG icon (w-3 h-3) before "Verified" text. Title attribute: `On-chain proof: ${txHash}`.

2. **txHash is null (gray "TEE" badge):** Render as a `<span>`. Style: same layout classes but bg-gray-100, text-gray-500. No link. Title attribute: "Processed in TEE -- on-chain proof pending". Text: "TEE".

Use a small shield SVG icon for the Verified state (path: M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z). No icon for the TEE-only state (keep it minimal).

Export as default.

**TaskCard.tsx:**
1. Import VerifyBadge from `./VerifyBadge`.
2. In the right-side area of the card (next to the delete button), add `<VerifyBadge txHash={task.txHash} />` between the content and the delete button.
3. The badges + delete area should be: `flex items-center gap-1 flex-shrink-0`.
4. Ensure the VerifyBadge does not break layout -- it should sit neatly in the top-right of the card alongside the delete X button.
  </action>
  <verify>Run `pnpm compile` -- zero type errors. Run `pnpm build` -- successful build. Inspect that VerifyBadge.tsx exists with both badge states and explorer URL. Inspect that TaskCard.tsx imports and renders VerifyBadge.</verify>
  <done>VerifyBadge component renders green "Verified" (with explorer link) when txHash present, gray "TEE" when null. TaskCard displays the badge next to the delete button. pnpm build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 3 UI</name>
  <files>none</files>
  <action>
Human visually verifies the complete Phase 3 UI. This is a checkpoint -- no code changes, only verification of the work done in Plan 01 and Plan 02 Task 1.

What was built: Complete task management UI with CRUD operations and TEE verification badges. This covers all of Phase 3: task list display with action text/deadline/source, checkbox completion with visual state change (line-through + opacity), delete functionality, green "Verified" badge linking to OpenGradient explorer (for tasks with txHash), and gray "TEE" badge (for tasks without txHash). All styled with Tailwind CSS classes.

Steps to verify:
1. Run `pnpm dev` to start the extension in development mode
2. Load the extension in Chrome (chrome://extensions, load unpacked from .output/chrome-mv3-dev)
3. If no tasks exist yet: paste some text content and click "Extract from Clipboard" to create tasks (proxy must be running: `node proxy.mjs`)
4. Verify task list: each task shows action text (bold), type badge (e.g. "Task"), priority badge (colored), deadline if present, and source
5. Verify completion: click the checkbox on a task -- text should get line-through and card should dim (opacity). Click again to un-complete. Close and reopen popup -- completed state should persist.
6. Verify deletion: click the X button on a task -- it should disappear. Close and reopen popup -- deleted task should stay gone.
7. Verify TEE badge: tasks extracted via AI should show either green "Verified" badge (if txHash present -- click it, should open explorer.opengradient.ai/tx/...) or gray "TEE" badge (if no txHash).
8. Verify wallet key setup: if key not configured, the input section should appear. After entering key, it should show "Configured" and the extraction button should appear.
9. Verify no inline styles remain: all UI elements should use Tailwind utility classes.
  </action>
  <verify>Human confirms all 9 verification steps pass by typing "approved".</verify>
  <done>User has visually verified the complete Phase 3 UI and typed "approved".</done>
</task>

</tasks>

<verification>
1. `pnpm compile` passes with zero errors
2. `pnpm build` produces a successful build
3. VerifyBadge.tsx renders two states: green "Verified" with explorer link, gray "TEE" without link
4. TaskCard includes VerifyBadge in its layout
5. Explorer link format: `https://explorer.opengradient.ai/tx/${txHash}`
6. Human verifies visual appearance and interactive behavior of the complete Phase 3 UI
</verification>

<success_criteria>
- Every task shows a TEE verification badge (green Verified or gray TEE)
- Green badge links to OpenGradient explorer with correct txHash URL
- Gray badge shows "TEE" text with tooltip explaining on-chain proof pending
- Complete Phase 3 passes human visual verification: task list, CRUD, badges all working
</success_criteria>

<output>
After completion, create `.planning/phases/03-task-ui-privacy-verification/03-02-SUMMARY.md`
</output>
